请基于下面这些 Emacs 引用上下文工作，不要要求我重复粘贴源码，也不要依赖复制出来的大段代码。

把这些引用当作权威上下文；优先使用 @file、@range、@line、@defun、@block 中的文件/行/列位置定位代码。保持项目级视野，但修改要尽量小块、逐文件或逐范围推进。

{{task-section}}

引用:

{{references}}

输出要求:

- 如果需要改代码或 prompt，先说明将修改的文件/prompt/行列范围和具体改法，等待用户批准
- 每个修改块单独申请批准，不要一次性做宽泛的项目级操作
- 获批后再执行该块修改，并简要说明实际改动和剩余风险
