# -*- mode: snippet -*-
# name: Org note block
# key: note
# -- Org special block
# --
#+begin_note
$0
#+end_note `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`
