# -*- mode: snippet -*-
# name: Org shell src block
# key: srcsh
# --
#+begin_src sh${1:}
$0
#+end_src `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`
