# -*- mode: snippet -*-
# name: Org theorem alias block
# key: thm
# -- Org special block alias
# --
#+begin_thm
$0
#+end_thm `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`
