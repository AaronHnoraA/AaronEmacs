# -*- mode: snippet -*-
# name: Org definition block
# key: definition
# -- Org special block
# --
#+begin_definition
$0
#+end_definition `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`
