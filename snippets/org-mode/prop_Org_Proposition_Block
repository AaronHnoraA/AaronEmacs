# -*- mode: snippet -*-
# name: Org proposition block
# key: prop
# -- Org special block
# --
#+begin_prop
$0
#+end_prop `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`
