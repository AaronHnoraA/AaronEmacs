# -*- mode: snippet -*-
# name: Org theorem block
# key: theorem
# -- Org special block
# --
#+begin_theorem
$0
#+end_theorem `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`
