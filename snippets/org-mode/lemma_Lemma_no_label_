# -*- mode: snippet -*-
# name: Org lemma block
# key: lemma
# -- Org special block
# --
#+begin_lemma
$0
#+end_lemma `(when (and (not (eobp)) (looking-at "
")) (let ((inhibit-modification-hooks t)) (ignore-errors (delete-char 1))))`
