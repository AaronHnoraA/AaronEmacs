;;; init-ignored.el --- Reserved compatibility module -*- lexical-binding: t; -*-

;;; Commentary:
;; This file intentionally stays lightweight so `init-modules.el` keeps a stable
;; load order while experimental code can be added here temporarily if needed.

;;; Code:

(provide 'init-ignored)
;;; init-ignored.el ends here
